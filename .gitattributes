# Git LFS pour les gros fichiers de modèles (OBSOLETE - maintenant sur GCS)
# Les modèles sont hébergés sur Google Cloud Storage : gs://air-paradis-models/
# Plus besoin de Git LFS pour éviter les limitations de budget GitHub

# Ancien config LFS (gardé pour historique)
# *.h5 filter=lfs diff=lfs merge=lfs -text
# *.hdf5 filter=lfs diff=lfs merge=lfs -text
# *.pickle filter=lfs diff=lfs merge=lfs -text
# *.pkl filter=lfs diff=lfs merge=lfs -text

# Autres fichiers volumineux potentiels (si nécessaire à l'avenir)
*.bin filter=lfs diff=lfs merge=lfs -text
*.model filter=lfs diff=lfs merge=lfs -text
*.weights filter=lfs diff=lfs merge=lfs -text

# Archives (si nécessaire)
*.zip filter=lfs diff=lfs merge=lfs -text
*.tar.gz filter=lfs diff=lfs merge=lfs -text
